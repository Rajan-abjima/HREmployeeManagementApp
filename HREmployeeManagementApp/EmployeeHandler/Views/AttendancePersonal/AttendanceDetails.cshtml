@model AttendanceViewModel

@section head{
	<link href="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" rel="stylesheet" id="bootstrap-css">
	<link href="//cdn.datatables.net/1.13.6/css/jquery.dataTables.min.css" rel="stylesheet">
	<link href='https://use.fontawesome.com/releases/v5.7.2/css/all.css' rel='stylesheet'>
	<script src="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js"></script>
	<script src="//cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
	<script src="https://code.jquery.com/jquery-3.7.0.js"></script>
	<script src="//cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
	<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-modal/0.9.1/jquery.modal.min.js"></script>
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/jquery-modal/0.9.1/jquery.modal.min.css" />
}

@{
	var employeeID = Context.Request.Query["EmployeeID"];
}

<style>
	table {
		table-layout: fixed;
		width: 100%;
	}

	.modal {
		height: fit-content !important;
		padding: 0;
		border: 2px #0062cc solid !important;
	}

	.close-modal {
		display: none !important;
	}

	.regularizelink {
	}

	.regularize:hover > td > .regularizelink {
		visibility: visible;
	}
</style>

<section id="tabs" class="project-tab">
	<div class="container">
		<div class="row">
			<div class="col-md-11">
				<nav>
					<div class="nav nav-tabs nav-fill" id="nav-tab" role="tablist">
						<a class="nav-item nav-link active" id="nav-home-tab" data-toggle="tab" role="tab" aria-controls="nav-home" aria-selected="true">Attendance Details</a>
						<a href="@Url.Action("Leave", "AttendancePersonal", new {EmployeeID = employeeID})" class="nav-item nav-link">Apply Leave</a>
						<a href="@Url.Action("RegularizationForm", "AttendancePersonal", new {EmployeeID = employeeID})" class="nav-item nav-link">Regularization Form</a>
					</div>
				</nav>

				<div class="tab-content" id="nav-tabContent">
					<div class="tab-pane fade show active row" id="nav-home" role="tabpanel" aria-labelledby="nav-home-tab">
						<table id="myTable" class="table table-striped table-light table-hover col-lg-auto" cellspacing="0">
							<thead>
								<tr>
									<th>Date</th>
									<th>Check In</th>
									<th>Check Out</th>
									<th>Status</th>
									<th>Modified</th>
									<th></th>
								</tr>
							</thead>
							<tbody>
								@*<tr>
								<td>Date</td>
								<td>CheckIn</td>
								<td>CheckOut</td>
								<td>Status</td>
								<td>Modified</td>
								</tr>*@
							</tbody>
						</table>
						<div id="regularizeModal" style="display:none">

						</div>
						@*<tbody id="" data-employee-id="@TempData["EmployeeIDForAttendance"]">

						<!------------------------Partially Adding Attendance List based on filters---------------------------->
						</tbody>*@
					</div>
				</div>
			</div>
			<div class="col-1">
				<button class="btn btn-light" id="allFilters" type="button" style="float:right;" data-bs-toggle="popover" data-bs-placement="bottom" data-bs-title="Filters">
					<img src="~/funnel.svg" width="25px" height="auto" />
				</button>
			</div>
			<div hidden>
				<div data-name="popover-content">
					<div>
						<input class="form-control form-control-sm mt-1" type="date" id="fromDate" />
						<input class="form-control form-control-sm mt-1" type="date" id="toDate" />
						<select class="dropdown mt-1" style="width:100%" id="statusFilter">
							<option value="">All</option>
							<option value="Present">Present</option>
							<option value="Absent">Absent</option>
							<option value="Leave">Leave</option>
						</select><br>
						<button type="button" class="btn btn-outline-dark mt-1" onclick="filterTable()">Filter</button>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

@section Scripts
	{
	<script src="~/lib/datatables/js/jquery.dataTables.min.js"></script>
	<script src="~/lib/datatables/js/dataTables.bootstrap4.min.js"></script>


	<script>
		function filterTable() {
			//var fromDate = document.getElementById("fromDate").value;
			//var toDate = document.getElementById("toDate").value;
			//var statusFilter = document.getElementById("statusFilter").value;
			var employeeID = @TempData["EmployeeIDForAttendance"];

			$("#myTable tbody tr").remove();
			$.ajax({
				type: "GET",
				url: "/AttendancePersonal/AttendanceDetailsPartial",
				dataType: 'json',
				data: {
					employeeID: employeeID
					//fromDate: fromDate,
					//toDate: toDate,
					//status: statusFilter,
				},
				success: function (data) {

					var rows = "";
					for (var x = 0; x < data.length; x++) {
						rows += "<tr class='Regularize'>"
						rows += "<td>" + data[x].date.substring(0, 10) + "</td>"
						rows += "<td>" + data[x].checkIn.substring(11, 19) + "</td>"
						rows += "<td>" + data[x].checkOut.substring(11, 19) + "</td>"
						rows += "<td>" + data[x].status + "</td>"
						rows += "<td>" + data[x].modifiedBy + "</td>"
						rows += "<td><button onClick='regularize("+data[x].attendanceID+")' class='btn btn-outline-primary regularize'>Regularize</button></td>"
						//rows += '<td> <a  asp-action="RegularizeEntryFormPartial" asp-route-EmployeeID='+data[x].employeeID+'>Regularize</a></td>'
						//rows += "<td> <a href='Url.Action("RegularizeFromEntryPartial",new{AttendanceID="+data[x].attendanceID+"})' rel='modal-open'>Regularize</a></td>"
						//rows += "<td> <a href='/AttendancePersonal/RegularizeEntryFormPartial?EmployeeID="+data[x].employeeID+"&AttendanceID="+data[x].attendanceID+"' rel='modal-open'>Regularize</a></td>"
						rows += "</tr>";
					};
					$('#myTable tbody').html(rows);
					$('#myTable').dataTable({
					});
				},
				error: function (ex) {
					//console.log(error);
					var r = jQuery.parseJSON(response.responseText);
					alert("Message: " + r.Message);
					alert("StackTrace: " + r.StackTrace);
					alert("ExceptionType: " + r.ExceptionType);
				}
			});
		}

		$(document).ready(function () {
			$("#modalbtn").click(function () {

			})
		})

		//function regularize(data) {
		//	const myJson = JSON.stringify(data);
		//	console.log(myJson);

			$(function () {
				$("#regularizeModal").dialog({
					autoOpen: false,
					modal: true,
					title: "View Details"
				});
				$("#mytable .regularize").click(function regularize(data) {
					var attendanceID = JSON.stringify(data);
					$.ajax({
						type: "GET",
						url: "/AttendancePersonal/RegularizeEntryFormPartial",
						data: { attendanceID: attendanceID,
								employeeID: @employeeID
						},
						success: function (response) {
							$('#regularizeModal').html(response);
							$('#regularizeModal').dialog('open');
						},
						failure: function (response) {
							alert(response.responseText);
						},
						error: function (response) {
							alert(response.responseText);
						}
					});
				});
			});

		$(document).ready(function () {
			filterTable();
		});
	</script>

	<script>
		$(document).ready(function () {

			var options = {
				html: true,
				content: $('[data-name="popover-content"]')
			}
			var exampleEl = document.getElementById('allFilters')
			var popover = new bootstrap.Popover(exampleEl, options)
		})
	</script>

	<script>
		$(document).ready(function () {
			//$('#myTable').dataTable({
			//	//data:data
			//});
		});
	</script>
}




